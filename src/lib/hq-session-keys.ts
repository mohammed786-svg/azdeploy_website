/** sessionStorage key for Django HQ API Bearer token (cross-origin). */
export const HQ_API_SESSION_STORAGE_KEY = "hq_api_session";

/** localStorage fallback so new tabs can reuse HQ API bearer. */
export const HQ_API_LOCAL_STORAGE_KEY = "hq_api_session_local";
